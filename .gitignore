# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Next.js
.next/
out/

# Production
build/
dist/

# Misc
.DS_Store
*.pem

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Solana Specific
target/
deploy/
test-ledger/

# Secrets and sensitive data
secrets/
*.json
!package.json
!tsconfig.json
!next.config.json
!Anchor.toml
!messages/*.json
!lib/idl/*.json
!vercel.json
!public/actions.json

# Exclusion for specific folders that might contain json we WANT to keep
# But since wallets/ are keys, we should BE CAREFUL.
# Typically wallets/*.json SHOULD BE IGNORED if they are real keys.
# The task asked to configure .gitignore (exclude node_modules, .next, .env, secrets)
